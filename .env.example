# Copiar a .env y ajustar si es necesario:
#   copy .env.example .env   (Windows)
#   cp .env.example .env     (Linux/Mac)

# Aplicación
APP_DEBUG=1

# Base de datos
# En Docker: el compose ya define DB_HOST=db para el contenedor php.
# En local (tests, scripts): usar 127.0.0.1 si MySQL corre en tu máquina.
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=fx_platform
DB_USER=fx_user
DB_PASS=fx_password

# Logs
LOG_PATH=logs
LOG_LEVEL=DEBUG

# WebSockets (puertos dentro de Docker)
WS_PRICES_PORT=8081
WS_CONFIG_PORT=8082

# Generador de precios (URL del servicio en Docker)
PRICE_GENERATOR_URL=http://price-generator:5000
